# PCB

Place the matrix centrally, row/column drivers immediately behind it, and the ESP32 connector away from high-current LED routing. Route 5V/GND first, then LED current paths, then logic.

The actual PCB file must be created in EasyEDA/KiCad and DRC-checked before fabrication.
